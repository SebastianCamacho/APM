<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:UI.ViewModels"
             xmlns:model="clr-namespace:AppsielPrintManager.Core.Models;assembly=Core"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="UI.Views.PrintersPage"
             Title="Impresoras"
             x:DataType="viewmodels:PrintersViewModel">

    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- Efecto Hover Dinámico para Ocultar/Mostrar Iconos en Windows -->
            <Style x:Key="ActionIconStyle" TargetType="ImageButton">
                <!-- Por defecto en Android siempre es visible, en Windows los ocultamos para el Hover -->
                <Setter Property="IsVisible" Value="{OnPlatform Android=True, Default=False}" />
                <Setter Property="Opacity" Value="0.6" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="PointerOver">
                                <VisualState.Setters>
                                    <Setter Property="Opacity" Value="1.0" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <!-- Estilo Hover para la Tarjeta de la Impresora (Revela las acciones hijas) -->
            <Style x:Key="PrinterCardStyle" TargetType="Border">
                <Setter Property="StrokeShape" Value="RoundRectangle 12" />
                <Setter Property="Stroke" Value="{AppThemeBinding Light=#EAECEE, Dark=#3A3F45}" />
                <Setter Property="StrokeThickness" Value="1.0" />
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=White, Dark=#2D3033}" />
                <Setter Property="Padding" Value="20" />
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="Stroke" Value="{AppThemeBinding Light=#EAECEE, Dark=#3A3F45}" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="PointerOver">
                                <VisualState.Setters>
                                    <Setter Property="Stroke" Value="{DynamicResource PrimaryLight}" />
                                    <!-- Truco: En Windows forzamos un color leve al fondo también -->
                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FCFBFF, Dark=#33313B}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid BackgroundColor="{AppThemeBinding Light=#F5F7FA, Dark=#1A1C1E}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header Toolbar Falso -->
        <Border Grid.Row="0" StrokeThickness="0" BackgroundColor="{AppThemeBinding Light=White, Dark=#2D3033}" Padding="30,15" Margin="0,0,0,5">
            <Border.Shadow>
                <Shadow Brush="#000000" Offset="0,2" Radius="5" Opacity="0.05" />
            </Border.Shadow>
            <Grid ColumnDefinitions="*, Auto">
                <Label Grid.Column="0" Text="Impresoras" FontSize="14" TextColor="{AppThemeBinding Light=#5A6A85, Dark=#A9B4C2}" VerticalOptions="Center" />
                <!-- Status Indicator with Pulses (Omitido por ahora si no está en VM, usar hardcoded o bindings reales) -->
                <HorizontalStackLayout Grid.Column="1" Spacing="8" VerticalOptions="Center">
                    <Ellipse Fill="#10B981" WidthRequest="10" HeightRequest="10" VerticalOptions="Center" />
                    <Label Text="Servicio Activo" FontSize="13" TextColor="{AppThemeBinding Light=#333A45, Dark=#E1E5EA}" FontAttributes="Bold" VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Grid>
        </Border>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="30,20" Spacing="25">
                
                <!-- Título y Botón Añadir -->
                <Grid ColumnDefinitions="*, Auto">
                    <Label Grid.Column="0" Text="Impresoras" FontSize="26" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#1D263B, Dark=White}" VerticalOptions="Center" />
                    
                    <Button Grid.Column="1" 
                            Text="+ Añadir" 
                            Command="{Binding AddPrinterCommand}" 
                            BackgroundColor="{DynamicResource Primary}" 
                            TextColor="White" 
                            FontAttributes="Bold" 
                            CornerRadius="8" 
                            Padding="20,10"
                            HeightRequest="42" />
                </Grid>

                <!-- Vista Vacía -->
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center"
                                     Padding="40" Spacing="15" IsVisible="{Binding HasNoPrinters}">
                    <Image Source="icono_impresora.png" HeightRequest="60" WidthRequest="60" HorizontalOptions="Center" Opacity="0.3" />
                    <Label Text="No hay impresoras configuradas aún." FontSize="16" TextColor="Gray" HorizontalOptions="Center" />
                </VerticalStackLayout>

                <!-- Lista Responsiva (Grid Windows, Block Android) -->
                <CollectionView ItemsSource="{Binding Printers}" SelectionMode="None"
                                IsVisible="{Binding HasNoPrinters, Converter={StaticResource InverseBoolConverter}}">
                    
                    <!-- Magia FlexLayout para Responsive Design -->
                    <CollectionView.ItemsLayout>
                        <!-- En Windows se comportará como un CSS Flex Wrap al indicarle ItemSpacing -->
                        <GridItemsLayout Orientation="Vertical" Span="{OnPlatform Android=1, Default=2}" HorizontalItemSpacing="20" VerticalItemSpacing="20" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="model:PrinterSettings">
                            <!-- SwipeView reservado solo para Android/Móvil si lo desean, lo dejamos base -->
                            <SwipeView>
                                <SwipeView.RightItems>
                                    <SwipeItems Mode="Reveal">
                                        <SwipeItemView Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PrintersViewModel}}, Path=EditPrinterCommand}"
                                                       CommandParameter="{Binding .}">
                                            <Grid BackgroundColor="{DynamicResource Primary}" WidthRequest="80" Margin="0,0,5,0">
                                                <Label Text="Editar" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" />
                                            </Grid>
                                        </SwipeItemView>
                                        <SwipeItemView Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PrintersViewModel}}, Path=DeletePrinterCommand}"
                                                       CommandParameter="{Binding .}">
                                            <Grid BackgroundColor="#EF4444" WidthRequest="80" Margin="0,0,5,0">
                                                <Label Text="Eliminar" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" />
                                            </Grid>
                                        </SwipeItemView>
                                    </SwipeItems>
                                </SwipeView.RightItems>
                                
                                <Border Style="{StaticResource PrinterCardStyle}">
                                    <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="15">
                                        
                                        <!-- Cajita Lila del Ícono -->
                                        <Border Grid.Column="0" StrokeThickness="0" BackgroundColor="{AppThemeBinding Light=#F4F0FF, Dark=#2A2542}" StrokeShape="RoundRectangle 12" WidthRequest="55" HeightRequest="55" VerticalOptions="Start">
                                            <Image Source="icono_impresora.png" HeightRequest="28" WidthRequest="28" HorizontalOptions="Center" VerticalOptions="Center">
                                                <Image.Behaviors>
                                                    <toolkit:IconTintColorBehavior TintColor="{DynamicResource Primary}" />
                                                </Image.Behaviors>
                                            </Image>
                                        </Border>

                                        <!-- Información Central -->
                                        <VerticalStackLayout Grid.Column="1" Spacing="8" VerticalOptions="Center">
                                            <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                                                <Label Text="{Binding PrinterId}" FontAttributes="Bold" FontSize="18" TextColor="{AppThemeBinding Light=#1D263B, Dark=White}" VerticalOptions="Center" />
                                                
                                                <!-- Badges de Tipo y Conexión -->
                                                <Border StrokeShape="RoundRectangle 6" StrokeThickness="0" BackgroundColor="{AppThemeBinding Light=#F4F0FF, Dark=#2A2542}" Padding="6,2" VerticalOptions="Center">
                                                    <Label Text="{Binding PrinterType, StringFormat='{0}'}" FontSize="10" TextColor="{DynamicResource Primary}" FontAttributes="Bold" />
                                                </Border>
                                                <Border StrokeShape="RoundRectangle 6" StrokeThickness="0" BackgroundColor="{AppThemeBinding Light=#F0F2F5, Dark=#3A3F45}" Padding="6,2" VerticalOptions="Center">
                                                    <Label Text="{Binding ConnectionType}" FontSize="10" TextColor="{AppThemeBinding Light=#5A6A85, Dark=#A9B4C2}" FontAttributes="Bold" />
                                                </Border>
                                            </HorizontalStackLayout>
                                            
                                            <!-- IP y Detalles -->
                                            <HorizontalStackLayout Spacing="5">
                                                <Label Text="{Binding IpAddress}" TextColor="{AppThemeBinding Light=#5A6A85, Dark=#A9B4C2}" FontSize="13">
                                                    <Label.Triggers>
                                                        <DataTrigger TargetType="Label" Binding="{Binding ConnectionType}" Value="USB">
                                                            <Setter Property="IsVisible" Value="False" />
                                                        </DataTrigger>
                                                    </Label.Triggers>
                                                </Label>
                                                <Label Text="{Binding Port, StringFormat=':{0}'}" TextColor="{AppThemeBinding Light=#5A6A85, Dark=#A9B4C2}" FontSize="13">
                                                    <Label.Triggers>
                                                        <DataTrigger TargetType="Label" Binding="{Binding ConnectionType}" Value="USB">
                                                            <Setter Property="IsVisible" Value="False" />
                                                        </DataTrigger>
                                                        <DataTrigger TargetType="Label" Binding="{Binding ConnectionType}" Value="IPP">
                                                            <Setter Property="IsVisible" Value="False" />
                                                        </DataTrigger>
                                                    </Label.Triggers>
                                                </Label>
                                                <Label Text="{Binding LocalPrinterName}" TextColor="{AppThemeBinding Light=#5A6A85, Dark=#A9B4C2}" FontSize="13" IsVisible="False">
                                                    <Label.Triggers>
                                                        <DataTrigger TargetType="Label" Binding="{Binding ConnectionType}" Value="USB">
                                                            <Setter Property="IsVisible" Value="True" />
                                                        </DataTrigger>
                                                    </Label.Triggers>
                                                </Label>
                                                <Label Text="{Binding Uri}" TextColor="{AppThemeBinding Light=#5A6A85, Dark=#A9B4C2}" FontSize="13" IsVisible="False">
                                                    <Label.Triggers>
                                                        <DataTrigger TargetType="Label" Binding="{Binding ConnectionType}" Value="IPP">
                                                            <Setter Property="IsVisible" Value="True" />
                                                        </DataTrigger>
                                                    </Label.Triggers>
                                                </Label>
                                            </HorizontalStackLayout>

                                            <!-- Ancho de Papel -->
                                            <Label Text="{Binding PaperWidthMm, StringFormat='Ancho: {0}mm'}" TextColor="{AppThemeBinding Light=#8E9EAA, Dark=#8A949E}" FontSize="13" IsVisible="False">
                                                <Label.Triggers>
                                                    <DataTrigger TargetType="Label" Binding="{Binding PrinterType}" Value="Térmica">
                                                        <Setter Property="IsVisible" Value="True" />
                                                    </DataTrigger>
                                                </Label.Triggers>
                                            </Label>
                                        </VerticalStackLayout>
                                        
                                        <!-- Iconos de Acción Flotantes (Visibles solo al hacer Hover en Windows) -->
                                        <!-- Nota: Para simplificar y evitar Behaviors incompatibles con Mac/Android nativo, usaremos visibilidad fija en Móvil y condicional en Desktop -->
                                        <HorizontalStackLayout Grid.Column="2" Spacing="8" VerticalOptions="Start" IsVisible="{OnPlatform Android=True, Default=True}">
                                            <!-- Botón Editar -->
                                            <ImageButton Source="icono_editar.png" 
                                                         Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PrintersViewModel}}, Path=EditPrinterCommand}"
                                                         CommandParameter="{Binding .}"
                                                         WidthRequest="32" HeightRequest="32" Padding="6" BackgroundColor="Transparent" CornerRadius="8">
                                                 <VisualStateManager.VisualStateGroups>
                                                    <VisualStateGroup x:Name="CommonStates">
                                                        <VisualState x:Name="Normal">
                                                            <VisualState.Setters>
                                                                <Setter Property="BackgroundColor" Value="Transparent" />
                                                            </VisualState.Setters>
                                                        </VisualState>
                                                        <VisualState x:Name="PointerOver">
                                                            <VisualState.Setters>
                                                                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#F4F0FF, Dark=#2A2542}" />
                                                            </VisualState.Setters>
                                                        </VisualState>
                                                    </VisualStateGroup>
                                                </VisualStateManager.VisualStateGroups>
                                            </ImageButton>

                                            <!-- Botón Eliminar -->
                                            <ImageButton Source="icono_eliminar_impresora.png" 
                                                         Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PrintersViewModel}}, Path=DeletePrinterCommand}"
                                                         CommandParameter="{Binding .}"
                                                         WidthRequest="32" HeightRequest="32" Padding="6" BackgroundColor="Transparent" CornerRadius="8">
                                                <VisualStateManager.VisualStateGroups>
                                                    <VisualStateGroup x:Name="CommonStates">
                                                        <VisualState x:Name="Normal">
                                                            <VisualState.Setters>
                                                                <Setter Property="BackgroundColor" Value="Transparent" />
                                                            </VisualState.Setters>
                                                        </VisualState>
                                                        <VisualState x:Name="PointerOver">
                                                            <VisualState.Setters>
                                                                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FEF2F2, Dark=#3F282A}" />
                                                            </VisualState.Setters>
                                                        </VisualState>
                                                    </VisualStateGroup>
                                                </VisualStateManager.VisualStateGroups>
                                            </ImageButton>
                                        </HorizontalStackLayout>
                                        
                                    </Grid>
                                </Border>
                            </SwipeView>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>
        <ActivityIndicator Grid.Row="1" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" VerticalOptions="Center" HorizontalOptions="Center" Color="{DynamicResource Primary}" />
    </Grid>
</ContentPage>
